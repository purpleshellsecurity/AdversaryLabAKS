{
  "rule": "detections/kql/lateral-movement.kql",
  "table": "AKSAudit",
  "inject_timegenerated": true,
  "note": "Tier 1 proves: a workload SA crossing namespaces fires; same-namespace use is silent; AND a legitimate kube-system controller acting cross-namespace is now suppressed by the infra_source_ns allowlist. That last case is the tuning that graduates this rule from Draft.",
  "schema": [
    { "name": "Verb", "type": "string" },
    { "name": "User", "type": "dynamic" },
    { "name": "ObjectRef", "type": "dynamic" }
  ],
  "cases": [
    { "name": "attacker-ns SA acting on victim-ns fires", "fixture": "malicious.json", "expect": "match" },
    { "name": "same-ns use and allowlisted kube-system controller stay silent", "fixture": "benign.json", "expect": "no_match" }
  ]
}
